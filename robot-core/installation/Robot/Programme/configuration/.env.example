# Modèle de configuration des secrets d'exécution.
# Copier ce fichier en « .env » sur la cible (Jetson) et y renseigner les vraies valeurs.
# Le vrai .env n'est JAMAIS versionné (voir .gitignore et la doc d'installation).
GOOGLE_SPEECH_API_KEY=
ANTHROPIC_API_KEY=
ROBOT_HOME=Robot/Programme
